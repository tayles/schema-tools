import { type NextPage } from 'next';
import Layout from '@/components/Layout';

const JsonFormatPage: NextPage = () => {
  return (
    <Layout title="Json Format">
      <p>Coming soon</p>
    </Layout>
  );
};

export default JsonFormatPage;

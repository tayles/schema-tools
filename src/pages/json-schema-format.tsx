import { type NextPage } from 'next';
import Layout from '@/components/Layout';

const JsonSchemaFormatPage: NextPage = () => {
  return (
    <Layout title="Json Schema Format">
      <p>Coming soon</p>
    </Layout>
  );
};

export default JsonSchemaFormatPage;
